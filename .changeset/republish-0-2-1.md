---
'@basex-ui/components': patch
'@basex-ui/tokens': patch
'@basex-ui/styles': patch
'@basex-ui/intelligence': patch
'@basex-ui/mcp-server': patch
'@basex-ui/cli': patch
---

Re-publish the initial release. v0.2.0 partially published (only `@basex-ui/mcp-server` made it through before npm scope/auth issues blocked the rest); this patch bumps everything to v0.2.1 so the remaining 5 packages can land on npm under the same release.
