---
title: AlertDialog
description: A modal dialog requiring explicit user acknowledgment for destructive or irreversible actions.
---

import { AlertDialogBasic } from '@/components/demos/alert-dialog-demo';

## Import

```tsx
import { AlertDialog } from '@basex-ui/components';
```

## Usage

<AlertDialogBasic />

```tsx
<AlertDialog.Root>
  <AlertDialog.Trigger render={<Button color="destructive">Delete</Button>} />
  <AlertDialog.Portal>
    <AlertDialog.Backdrop />
    <AlertDialog.Popup>
      <AlertDialog.Title>Delete this item?</AlertDialog.Title>
      <AlertDialog.Description>This action cannot be undone.</AlertDialog.Description>
      <AlertDialog.Actions>
        <AlertDialog.Close render={<Button variant="ghost">Cancel</Button>} />
        <AlertDialog.Close render={<Button color="destructive">Delete</Button>} />
      </AlertDialog.Actions>
    </AlertDialog.Popup>
  </AlertDialog.Portal>
</AlertDialog.Root>
```

## Parts

| Part | Description |
|------|-------------|
| `Root` | Controls open state |
| `Trigger` | Opens the dialog |
| `Portal` | Renders outside the DOM tree |
| `Backdrop` | Overlay behind the dialog |
| `Popup` | The dialog container |
| `Title` | Heading text |
| `Description` | Supporting text |
| `Actions` | Footer for action buttons |
| `Close` | Closes the dialog |

## Props

### Root

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controlled open state |
| `defaultOpen` | `boolean` | `false` | Initially open |
| `onOpenChange` | `(open) => void` | — | Called when open state changes |
